[pytest]
testpaths = tests services
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --strict-markers
    --cov=services
    --cov=shared
    --cov-report=html
    --cov-report=term-missing
asyncio_mode = auto

